% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{control.redeem}
\alias{control.redeem}
\title{Control Parameters for REDEEM Models}
\usage{
control.redeem(
  it_max = 100,
  tol = 1e-10,
  accelerated = FALSE,
  verbose = FALSE,
  weighting = TRUE,
  subsample = 1,
  build_time = NULL,
  use_glm = FALSE,
  return_data = FALSE,
  save_hist = TRUE,
  estimate = "Blockwise",
  legacy = FALSE,
  check_matrix = FALSE,
  inf_unidentifiable = TRUE
)
}
\arguments{
\item{it_max}{Integer; maximum number of iterations for the algorithm.
Defaults to 100.}

\item{tol}{Numeric; convergence tolerance. Defaults to 1e-10.}

\item{accelerated}{Logical; if \code{TRUE}, uses SQUAREM acceleration for
MM updates. Defaults to FALSE.}

\item{verbose}{Logical; if \code{TRUE}, prints progress information. Defaults to FALSE.}

\item{weighting}{Logical; whether to use weighting to group identical
observations. Defaults to TRUE.}

\item{subsample}{Numeric; proportion of data to subsample for internal
GLM checks. Defaults to 1.}

\item{build_time}{Numeric; time at which to start building the estimation
dataset. Events before this time are used to compute statistics but not
included as observations. Defaults to NULL, in which case all events are included.}

\item{use_glm}{Logical; if \code{TRUE}, uses standard GLM for updating
core coefficients. This is often slower but can yield more robust updates.
Defaults to FALSE.}

\item{return_data}{Logical; whether to return preprocessed data frames
in the result. Defaults to FALSE.}

\item{save_hist}{Logical; whether to save the iteration history of
coefficients. Defaults to TRUE.}

\item{estimate}{Character; estimation method for \code{\link{dem}} and
\code{\link{rem}} ("Blockwise", "NR", or "GD"). Defaults to "Blockwise".}

\item{legacy}{Logical; if \code{TRUE}, uses
a single \code{glm.fit} call instead of the iterative loop.
Defaults to FALSE.}

\item{check_matrix}{Logical; whether to apply \code{\link{check_matrix}}
to the event data before estimation. If \code{TRUE}, repairs missing
events (e.g., adding start events for interactions that only have end
events). Defaults to FALSE.}

\item{inf_unidentifiable}{Logical; whether to set unidentifiable
coefficients (e.g., actors with 0 event counts, globally
invariant/collinear covariates) to \code{-Inf}. Defaults to TRUE.}
}
\value{
A list of class \code{"redeem_control"} containing the specified
  parameters.
}
\description{
Unified control object to manage estimation parameters for \code{\link{rem}}
and \code{\link{dem}} functions.
}
